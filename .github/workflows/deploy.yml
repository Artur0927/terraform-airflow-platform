name: Deploy to EC2
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: SSH and Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.AIRFLOW_PRIVATE_IP }} # New Secret!
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          # Bastion / Jump Host Configuration
          proxy_host: ${{ secrets.BASTION_HOST }} # NEW Secret!
          proxy_username: ${{ secrets.USERNAME }}
          proxy_key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # ✅ FIX: Clone repo if it doesn't exist
            if [ ! -d "terraform-airflow-platform" ]; then
              echo "Repo not found. Cloning..."
              git clone https://github.com/Artur0927/terraform-airflow-platform.git
            fi
            cd terraform-airflow-platform
            git pull origin main
            cd airflow
            
            # Restart Containers
            sudo /usr/local/bin/docker-compose down
            sudo /usr/local/bin/docker-compose up -d --build
